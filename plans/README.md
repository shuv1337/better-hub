# Implementation Plans

Generated by the improve skill on 2026-06-25 against commit `6c18b28`. Execute in the order below unless dependencies say otherwise. Each executor must read the target plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bind prompt requests to their repo and viewer permissions | P1 | M | - | DONE |
| 002 | Encrypt stored user-supplied API keys | P1 | M | - | DONE |
| 003 | Fix GitHub user profile cache keying | P2 | S | - | DONE |
| 004 | Make background GitHub refreshes request-lifetime safe | P2 | M | 003 | DONE |
| 005 | Gate repo fragment cache reads after repo authorization | P2 | S | - | DONE |
| 006 | Clear high and critical dependency advisories | P2 | M | 001-005 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Overrides (Plan 006)

Root `package.json` `overrides` pin patched transitive versions when direct dependency bumps alone could not clear `bun audit --audit-level high`: `form-data`/`undici` via vercel/e2b/inngest; `vite`/`picomatch`/`minimatch` via vitest/@sentry/nextjs; `path-to-regexp`/`fast-uri` via vercel/prisma; `hono`/`lodash`/`tar`/`defu`/`drizzle-orm`/`protobufjs` via better-auth and AWS SDK trees.

## Dependency Notes

- 004 depends on 003 because both touch GitHub auth/cache paths in `apps/web/src/lib/github.ts` and `apps/web/src/lib/auth.ts`; landing the smaller auth-cache fix first keeps review small.
- 006 should land last because dependency updates can create unrelated type/test churn. Fix app-level security bugs first, then update packages with a clean test baseline.

## Findings Considered And Rejected

- None. The operator selected all six vetted findings.

